// use crate::hope::CoinfairState as HopeState;
// use teloxide::{dispatching::dialogue::InMemStorage, prelude::*};

// #[derive(Clone, Default, Debug)]
// pub enum BotState {
//     #[default]
//     Idle,
//     Hope(HopeState),
// }

// pub type HandlerResult = Result<(), Box<dyn std::error::Error + Send + Sync>>;
// pub type MainDialogue = Dialogue<BotState, InMemStorage<BotState>>;
